// PyScript Error Plugin
/* import { hooks } from "../core.js";
import npmImportCode from "./importNPM.py"

function installImportHook(wrap){
    wrap.run(npmImportCode);
}

hooks.main.onReady.add(function addImp(wrap) {
    // be sure this override happens only once
    hooks.main.onReady.delete(addImp);
    installImportHook(wrap);

});

hooks.worker.onReady.add(function addImp(wrap) {
    // be sure this override happens only once
    hooks.worker.onReady.delete(addImp);
    installImportHook(wrap);

}); */